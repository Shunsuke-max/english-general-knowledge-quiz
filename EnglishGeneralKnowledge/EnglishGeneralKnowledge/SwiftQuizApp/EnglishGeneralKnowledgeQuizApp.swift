import SwiftUI

// Keep this struct for reference when copying the SwiftUI quiz into a standalone target.
// Reapply `@main` in that context to make it an executable entry point.
struct EnglishGeneralKnowledgeQuizApp: App {
    var body: some Scene {
        WindowGroup {
            QuizRootView()
        }
    }
}
